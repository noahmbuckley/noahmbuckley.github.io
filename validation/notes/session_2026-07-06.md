# Validation PWA — session 2026-07-06

## TL;DR
Got the stalled/orphaned hand-coding tasks actually onto the iPad app and added
three new ones with divergence-first sampling. Registry went 14 → 20 tasks; every
task validated (both `validation/` and `docs/validation/` trees, all schemas parse,
every item carries its `id_field`); confirmed end-to-end render locally (picker + an
item view + form, no console errors). **Not pushed** — awaiting Noah's OK (public
GitHub Pages site).

## What changed
**Wired 3 already-built-but-unregistered tasks into `tasks.json` (both trees):**
- `sentiment-prompt-comparison` (200) — the standing "5th-audit" blocker; template
  existed since 2026-05-08 but fell out of `tasks.json` in the 2026-05-26 reboot.
  120/200 are model-disagreement rows; 80 all-agree anchor for a reweighted estimate.
- `rusSocial-responsibility-blind` (81) + `rusSocial-responsibility-focused` (20) —
  blame-attribution; PWA schemas already trimmed to 6/clear fields (the 30-col bloat
  was only in the old xlsx). Blind = no Haiku (unbiased κ); focused = Haiku confirm/correct.

(`channel-actor-A` + `courts-extract-A` were already registered in the working
`tasks.json`, just uncommitted — they ride along in the same commit.)

**Built 3 new tasks** via `build_2026-07-06_tasks.py` (seed=20260706, writes to both trees):
- `topics-A` (150) — campaign message topic gold, confirm/correct, 16-topic taxonomy,
  sorted hard-first (medium/low-confidence + multi-topic).
- `dedup-A` (150) — elite person-merge same/different, the 150 hardest of the 555
  (smallest `score_gap` to runner-up) + a small calibration draw.
- `disaster-date-A` (60) — does Haiku's quoted evidence support the extracted event
  date; uncertain-first (low/med confidence or `evidence_matches_event`≠True).

**Design principle applied:** divergent/hard-first ordering with a `stratum`/bucket tag
on every item, so Noah's labels are maximally informative AND an unbiased estimate is
recoverable by reweighting. LLM guesses shown as confirm/correct chips for speed, with
an anchoring caveat in the instructions.

## Verify
1. Open the app on the iPad after the push + a Refresh; the 6 new tasks should appear.
2. `topics-A` row 1 is a genuinely ambiguous case (governor signs a trolleybus-mfg deal,
   Haiku→agriculture_industry vs infrastructure_transport) — confirms hard-first worked.
3. Rebuild is reproducible: `/usr/bin/python3 build_2026-07-06_tasks.py`.

## Not done / next
- **Push** — needs Noah's go (outward-facing). Stage ONLY the validation task files
  (see the session chat for the exact `git add` list); leave unrelated dirty files
  (docs/search.json, sitemap, CV pdf, two other provenance.txt) out.
- **campaign_relevance gold** (the $300–1,500-ROI one) deferred: `message_topics_raw.jsonl`
  has per-message relevance preds but NO text column → needs a `post_id`→rusSocial text
  join before it can be a PWA task. ~30-min build, next.
